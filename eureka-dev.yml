server:
  port: 8084


eureka:
      client:
        registerWithEureka: false
        fetchRegistry: false

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream

logging:
  level:
    org.springframework.cloud: 'DEBUG'
