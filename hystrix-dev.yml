server:
  port: 8010

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8084/eureka/

management:
  endpoints:
    web:
      exposure:
        include: hystrix.stream

turbine:
  app-config: log,hystrix,gateway,business,config
  aggregator:
    cluster-config: default
  combine-host-port: true

logging:
  level:
    org.springframework.cloud: 'DEBUG'
